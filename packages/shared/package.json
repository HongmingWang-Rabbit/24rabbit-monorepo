{
  "name": "@24rabbit/shared",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf node_modules .turbo"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.16",
    "vitest": "^4.0.16",
    "@types/node": "^20.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
